# Employee Management System

This workspace contains a `client` (frontend) and `server` (backend) scaffold for an employee management system.

- Frontend: `client/` (React app stubs under `src/`)
- Backend: `server/` (Express + Mongoose stubs under `server/src/`)

Files and folders were created as placeholders to be filled with real implementation.
